<components:AppUserControl
    x:Class="LHQ.App.Views.ElementDebugInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:LHQ.App.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:LHQ.App.Model"
    d:DataContext="{d:DesignInstance model:ITreeElementViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <components:AppUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LHQ.App;component/Resources/Converters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </components:AppUserControl.Resources>
    <!--<Grid>
        <TextBlock Foreground="Silver"
                   Visibility="{Binding Converter={StaticResource DebugOnlyVisibilityConverter}}">
            <Run Text="Key:" />
            <Run Text="{Binding Path=ElementKey, Mode=OneWay}" />

            <Run Text="Version:" />
            <Run Text="{Binding Path=Version, Mode=OneWay}" />
        </TextBlock>
    </Grid>-->
</components:AppUserControl>
